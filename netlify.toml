[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback—safe to keep even if not using React Router yet
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
