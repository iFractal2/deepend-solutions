[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback (safe even if you’re not using React Router yet)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
